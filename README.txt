
# put your data in a folder called "data" 
  - n.b. that all files in './data' need to be in the same format (all time-lapse, or all single stacks, etc. note that a different number of slices per stack is fine)

# put your test images in a folder called "test" 
  - n.b. that all files in './test' need to be in the same format (all time-lapse, or all single stacks, etc. note that a different number of slices per stack is fine)

# set the hyperparameters.txt file

# type: " python train.py " to train your model

# type: " python predict.py " to perform one ZAugNet interpolation on your test images

